const bookDisplayContainer = document.querySelector(".book-display-container");

let library = [];

function Book(author, title, noOfPages, read) {
    // the constructor
    this.author = author;
    this.title = title;
    this.noOfPages = noOfPages;
    this.read = read;
}

function addBookToLibrary() {
    // add book to library
}

function displayBooks() {
    // function to display book in labrary
}